apiVersion: v1
kind: ServiceAccount
metadata:
  name: kpack-service-account
  namespace: default
secrets:
  - name: docker-credentials
  - name: github-creds
imagePullSecrets:
  - name: docker-credentials

